schema {
  query: RPSLQuery
}

enum RPKIStatus {
  valid
  invalid
  not_found
}

type RPSLQuery {
  route(origin: Int): [RPSLRoute]
}

type RPSLRoute {
  prefix: String
  origin: String
  descr: String
  remarks: String
  mntBy: [String]
  source: String
  rpkiStatus: RPKIStatus
}
